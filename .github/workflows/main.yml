name: Tests

on: push

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: bhrajesh97/deployer@4.3.0
      - name: Install modules
        run: npm install
      - name: Run Tests
        run: npm test
